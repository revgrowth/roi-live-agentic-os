#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
REPO="$(cd "$ROOT/../../.." && pwd)"
export PYTHONPATH="${ROOT}:${REPO}/.claude/skills/tool-tempo-efficiency:${REPO}/.claude/skills/tool-polaris-df${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m jev_dispatcher "$@"
