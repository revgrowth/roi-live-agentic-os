# Optional: absolute path to the AgenticOS repo root.
#
# Auto-detected when unset. Only set this if you need command-centre to point
# at a different checkout or data location than the repo it is running from.
#
# AGENTIC_OS_DIR=/absolute/path/to/agentic-os
