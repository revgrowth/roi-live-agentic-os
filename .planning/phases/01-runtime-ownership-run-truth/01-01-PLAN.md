---
phase: 01-runtime-ownership-run-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/briefs/command-centre/src/types/cron.ts
  - projects/briefs/command-centre/src/lib/cron-runtime.js
  - projects/briefs/command-centre/src/lib/cron-service.ts
  - projects/briefs/command-centre/src/lib/cron-system-status.ts
autonomous: true
requirements: [OWNR-01, OWNR-04, SAFE-02]
user_setup: []
must_haves:
  truths:
    - "Runtime ownership is derived from one shared contract that distinguishes active leader, stale leader, and no leader."
    - "Status consumers can tell when a runtime process exists without actually owning scheduling."
    - "Cron run metadata distinguishes observed completion from inferred recovery."
  artifacts:
    - path: "projects/briefs/command-centre/src/types/cron.ts"
      provides: "Shared runtime and cron-run truth types"
      contains: "leaderState"
    - path: "projects/briefs/command-centre/src/lib/cron-runtime.js"
      provides: "Canonical runtime-truth derivation and cron-run metadata defaults"
      contains: "statusSummary"
    - path: "projects/briefs/command-centre/src/lib/cron-system-status.ts"
      provides: "Shared runtime-status access point"
      contains: "getCronSystemStatus"
  key_links:
    - from: "projects/briefs/command-centre/src/lib/cron-system-status.ts"
      to: "projects/briefs/command-centre/src/lib/cron-runtime.js"
      via: "shared status helper"
      pattern: "getManaged.*RuntimeStatus|getManagedRuntimeStatus"
    - from: "projects/briefs/command-centre/src/lib/cron-service.ts"
      to: "projects/briefs/command-centre/src/lib/cron-runtime.js"
      via: "service wrapper"
      pattern: "getManagedRuntimeStatus|completeCronRunForTask"
---

<objective>
Define the shared runtime-truth and cron-run-truth contract that all later ownership surfaces must consume.

Purpose: Lock the semantics before UI, CLI, and recovery code each invent their own interpretation.
Output: Shared TypeScript/runtime contract for leader state, ownership summary, and completion-truth metadata.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-ownership-run-truth/01-CONTEXT.md
@.planning/phases/01-runtime-ownership-run-truth/01-RESEARCH.md
@projects/briefs/command-centre/src/types/cron.ts
@projects/briefs/command-centre/src/lib/cron-runtime.js
@projects/briefs/command-centre/src/lib/cron-service.ts
@projects/briefs/command-centre/src/lib/cron-system-status.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the shared runtime ownership contract</name>
  <files>projects/briefs/command-centre/src/types/cron.ts, projects/briefs/command-centre/src/lib/cron-runtime.js</files>
  <read_first>projects/briefs/command-centre/src/types/cron.ts, projects/briefs/command-centre/src/lib/cron-runtime.js, .planning/phases/01-runtime-ownership-run-truth/01-RESEARCH.md</read_first>
  <action>Extend the runtime status contract so `getManagedRuntimeStatus()` returns one canonical shape for daemon, in-process, and stopped states. Add explicit derived fields for `leaderState` (`active`, `stale`, `absent`), `localRuntimePresent`, `ownershipReason`, and `statusSummary`. Keep the existing lock-claim algorithm and lock file format stable; only centralize how raw lock/PID/heartbeat facts are interpreted for consumers.</action>
  <verify>cd projects/briefs/command-centre && npm run build</verify>
  <acceptance_criteria>
    - `projects/briefs/command-centre/src/types/cron.ts` contains `leaderState:`
    - `projects/briefs/command-centre/src/types/cron.ts` contains `statusSummary:`
    - `projects/briefs/command-centre/src/lib/cron-runtime.js` contains `ownershipReason`
    - `projects/briefs/command-centre/src/lib/cron-runtime.js` contains `localRuntimePresent`
  </acceptance_criteria>
  <done>The runtime contract exposes one reusable, derived ownership model without changing how leadership is claimed.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit cron-run truth metadata defaults</name>
  <files>projects/briefs/command-centre/src/types/cron.ts, projects/briefs/command-centre/src/lib/cron-runtime.js</files>
  <read_first>projects/briefs/command-centre/src/types/cron.ts, projects/briefs/command-centre/src/lib/cron-runtime.js, .planning/phases/01-runtime-ownership-run-truth/01-RESEARCH.md</read_first>
  <action>Extend the cron-run contract with nullable truth metadata fields that downstream code can persist without breaking existing rows. Add `resultSource` with concrete values `observed` and `inferred`, and add `completionReason` with concrete string values for normal completion vs recovery paths. Update runtime serialization helpers so existing rows safely default these fields to null and new rows can store them without schema ambiguity.</action>
  <verify>cd projects/briefs/command-centre && npm run build</verify>
  <acceptance_criteria>
    - `projects/briefs/command-centre/src/types/cron.ts` contains `resultSource`
    - `projects/briefs/command-centre/src/types/cron.ts` contains `completionReason`
    - `projects/briefs/command-centre/src/lib/cron-runtime.js` contains `resultSource`
    - `projects/briefs/command-centre/src/lib/cron-runtime.js` contains `completionReason`
  </acceptance_criteria>
  <done>The project has an explicit contract for observed vs inferred cron completion, ready for recovery logic to use in later plans of this phase.</done>
</task>

<task type="auto">
  <name>Task 3: Route all system-status reads through the canonical helper</name>
  <files>projects/briefs/command-centre/src/lib/cron-service.ts, projects/briefs/command-centre/src/lib/cron-system-status.ts</files>
  <read_first>projects/briefs/command-centre/src/lib/cron-service.ts, projects/briefs/command-centre/src/lib/cron-system-status.ts, projects/briefs/command-centre/src/lib/cron-runtime.js</read_first>
  <action>Refactor the service layer so there is one obvious entry point for runtime truth. `cron-system-status.ts` must stop being a meaningless pass-through and instead become the shared place that returns the normalized ownership contract. `cron-service.ts` should delegate to that shared helper rather than encouraging multiple ad-hoc interpretations of runtime fields.</action>
  <verify>cd projects/briefs/command-centre && npm run build</verify>
  <acceptance_criteria>
    - `projects/briefs/command-centre/src/lib/cron-system-status.ts` contains `statusSummary`
    - `projects/briefs/command-centre/src/lib/cron-service.ts` contains `getManagedCronRuntimeStatus`
    - `projects/briefs/command-centre/src/lib/cron-system-status.ts` is no longer a one-line pass-through
  </acceptance_criteria>
  <done>All runtime-status consumers have one shared backend truth source to build on.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd projects/briefs/command-centre && npm run build`
- [ ] `rg -n "leaderState|statusSummary|ownershipReason|resultSource|completionReason" projects/briefs/command-centre/src/types/cron.ts projects/briefs/command-centre/src/lib/cron-runtime.js projects/briefs/command-centre/src/lib/cron-system-status.ts`
</verification>

<success_criteria>

- All tasks completed
- The canonical runtime-truth contract exists in code
- Cron-run truth metadata exists for downstream recovery logic
- Backend status reads now share one normalized interpretation layer
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-ownership-run-truth/01-01-SUMMARY.md`
</output>
