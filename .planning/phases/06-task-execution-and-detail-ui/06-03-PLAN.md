---
phase: 06-task-execution-and-detail-ui
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - projects/briefs/command-centre/src/components/modal/task-modal.tsx
  - projects/briefs/command-centre/src/components/modal/modal-header.tsx
  - projects/briefs/command-centre/src/components/modal/modal-chat.tsx
  - projects/briefs/command-centre/src/components/modal/modal-sidebar.tsx
  - projects/briefs/command-centre/src/components/modal/chat-entry.tsx
  - projects/briefs/command-centre/src/components/modal/reply-input.tsx
  - projects/briefs/command-centre/src/store/task-store.ts
  - projects/briefs/command-centre/src/hooks/use-sse.ts
  - projects/briefs/command-centre/src/app/api/tasks/[id]/logs/route.ts
  - projects/briefs/command-centre/src/components/panel/task-detail-panel.tsx
autonomous: false
requirements:
  - EXEC-06-04
  - EXEC-06-05

must_haves:
  truths:
    - "Clicking a task card opens a full-screen modal (not the 480px slide-out)"
    - "Modal shows Claude output as styled chat bubbles with tool calls as collapsed cards"
    - "Live log entries stream into the chat view in real time via SSE"
    - "Auto-scroll follows new output; scrolling up pauses auto-scroll and shows Jump to latest"
    - "When Claude asks a question, a reply input appears and user can type a response"
    - "Modal shows task metadata (stats, outputs) in a sidebar"
  artifacts:
    - path: "projects/briefs/command-centre/src/components/modal/task-modal.tsx"
      provides: "Full-screen task modal container"
      contains: "TaskModal"
    - path: "projects/briefs/command-centre/src/components/modal/modal-chat.tsx"
      provides: "Chat view with log entries and auto-scroll"
      contains: "auto-scroll"
    - path: "projects/briefs/command-centre/src/components/modal/chat-entry.tsx"
      provides: "Individual log entry renderer (text bubble, tool card, question prompt)"
      contains: "LogEntryType"
    - path: "projects/briefs/command-centre/src/components/modal/reply-input.tsx"
      provides: "Reply input that sends to /api/tasks/[id]/reply"
      contains: "reply"
    - path: "projects/briefs/command-centre/src/app/api/tasks/[id]/logs/route.ts"
      provides: "GET endpoint returning log entries for a task"
      exports: ["GET"]
  key_links:
    - from: "task-modal.tsx"
      to: "task-store.ts"
      via: "selectedTaskId triggers modal open"
      pattern: "selectedTaskId"
    - from: "modal-chat.tsx"
      to: "use-sse.ts"
      via: "SSE task:log events append to chat"
      pattern: "task:log"
    - from: "reply-input.tsx"
      to: "/api/tasks/[id]/reply"
      via: "POST fetch on submit"
      pattern: "api/tasks"
    - from: "task-modal.tsx"
      to: "modal-sidebar.tsx"
      via: "PanelStats and PanelOutputs reuse"
      pattern: "PanelStats"
---

<objective>
Build the full-screen task modal with chat-style execution logs, live streaming, auto-scroll, and reply input for Claude questions.

Purpose: Per D-05 through D-10, this is essentially a clean UI terminal for the Claude session. Users see Claude's output as a chat conversation, can scroll through history, and reply when Claude asks questions. This replaces the 480px slide-out panel as the primary task detail view.
Output: Full-screen modal component suite, logs API, SSE integration for live log streaming, reply flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-task-execution-and-detail-ui/06-CONTEXT.md
@.planning/phases/06-task-execution-and-detail-ui/06-01-SUMMARY.md
@.planning/phases/06-task-execution-and-detail-ui/06-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: New types -->
<!-- LogEntry { id, type, timestamp, content, toolName?, toolArgs?, toolResult?, isCollapsed? } -->
<!-- LogEntryType = "text" | "tool_use" | "tool_result" | "question" | "user_reply" | "system" -->
<!-- TaskEvent now includes type: "task:question" with questionText?: string -->
<!-- processManager.getLogEntries(taskId) returns LogEntry[] -->
<!-- POST /api/tasks/[id]/reply accepts { message: string } -->

<!-- From Plan 02: Updated store -->
<!-- createTask(title, description, level) -->
<!-- Task.description: string | null -->

<!-- Existing reusable components -->
<!-- PanelStats from src/components/panel/panel-stats.tsx -- 2x2 grid of status/duration/cost/tokens -->
<!-- PanelOutputs from src/components/panel/panel-outputs.tsx -- file list with preview/download -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full-screen modal components and logs API</name>
  <files>
    projects/briefs/command-centre/src/components/modal/task-modal.tsx
    projects/briefs/command-centre/src/components/modal/modal-header.tsx
    projects/briefs/command-centre/src/components/modal/modal-chat.tsx
    projects/briefs/command-centre/src/components/modal/modal-sidebar.tsx
    projects/briefs/command-centre/src/components/modal/chat-entry.tsx
    projects/briefs/command-centre/src/components/modal/reply-input.tsx
    projects/briefs/command-centre/src/app/api/tasks/[id]/logs/route.ts
  </files>
  <read_first>
    projects/briefs/command-centre/src/components/panel/task-detail-panel.tsx
    projects/briefs/command-centre/src/components/panel/panel-stats.tsx
    projects/briefs/command-centre/src/components/panel/panel-outputs.tsx
    projects/briefs/command-centre/src/components/panel/panel-header.tsx
    projects/briefs/command-centre/src/types/task.ts
    projects/briefs/command-centre/src/store/task-store.ts
    projects/briefs/command-centre/src/lib/process-manager.ts
    projects/briefs/command-centre/src/hooks/use-sse.ts
    .planning/phases/06-task-execution-and-detail-ui/06-CONTEXT.md
  </read_first>
  <action>
**1. Create `src/app/api/tasks/[id]/logs/route.ts`** -- GET endpoint:
```typescript
import { NextRequest, NextResponse } from "next/server";
import { processManager } from "@/lib/process-manager";

export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ id: string }> }
) {
  const { id } = await params;
  const entries = processManager.getLogEntries(id);
  return NextResponse.json(entries);
}
```

**2. Create `src/components/modal/chat-entry.tsx`** -- renders a single LogEntry:

Based on `entry.type`:
- `"text"`: Styled as a Claude message bubble. Left-aligned. Background `#F6F3F1` (surface-container-low), `borderRadius: "0.75rem"`, padding `14px 18px`, max-width `85%`. Text in Inter 14px, color `#1B1C1B`, `line-height: 1.6`. Small "Claude" label above in Space Grotesk 11px, color `#5E5E65`. Render content as simple text (preserve newlines with `white-space: pre-wrap`).
- `"tool_use"`: Compact card, collapsed by default (per D-06). Show one line: a wrench icon (from lucide-react `Wrench`, 14px) + tool name in Space Grotesk 12px bold + first key arg value truncated to 40 chars. Background `#EAE8E6` (surface-container-high), `borderRadius: "0.375rem"`, padding `8px 12px`. Click toggles expanded state showing full `toolArgs` in a `<pre>` with Space Grotesk 12px, color `#5E5E65`, background `#F6F3F1`, padding 12px, borderRadius 0.25rem. Use local `useState` for collapsed toggle.
- `"tool_result"`: Only shown when parent tool_use is expanded. Gray text block with the result content. Space Grotesk 12px, color `#5E5E65`.
- `"question"`: Styled differently -- terracotta left border (3px solid `#93452A`), background `#FFF5F3` (light warm tint), padding `14px 18px`, borderRadius `0.5rem`. Question text in Inter 14px, fontWeight 500. Small "Claude is asking" label above in Space Grotesk 11px, color `#93452A`.
- `"user_reply"`: Right-aligned bubble. Background `#93452A` (terracotta), color white, borderRadius `0.75rem`, padding `12px 16px`, max-width `70%`. "You" label above in Space Grotesk 11px, color `#5E5E65`.
- `"system"`: Centered, small text. Space Grotesk 12px, color `#5E5E65`, fontStyle italic.

Each entry shows a timestamp (Space Grotesk 10px, color `#5E5E65`, opacity 0.6) below the bubble, formatted as "HH:MM:SS".

**3. Create `src/components/modal/modal-chat.tsx`** -- the main chat view:

Props: `taskId: string`, `logEntries: LogEntry[]`, `isRunning: boolean`, `questionText: string | null`

- Renders a scrollable container (`overflowY: "auto"`, flex: 1) with all log entries via `<ChatEntry>`
- **Auto-scroll with snap-back (per D-07):** Use a `useRef` for the scroll container and a `isAutoScrolling` state (default true). On new entries (`logEntries` length change), if `isAutoScrolling`, scroll to bottom. On user scroll: if `scrollTop + clientHeight < scrollHeight - 40` (scrolled up by more than 40px), set `isAutoScrolling = false`. If scrolled back to bottom, re-enable.
- When `isAutoScrolling === false`, show a "Jump to latest" button: fixed at bottom-center of the chat area, `position: sticky`, `bottom: 16px`, z-index 10. Styled: `backgroundColor: "rgba(252, 249, 247, 0.9)"`, `backdropFilter: "blur(8px)"`, `border: "1px solid rgba(218, 193, 185, 0.3)"`, `borderRadius: "1rem"`, padding `8px 16px`, Space Grotesk 12px, color `#93452A`, cursor pointer. Click scrolls to bottom and re-enables auto-scroll.
- When `isRunning` and no entries yet, show a typing indicator: three animated dots in `#93452A`.
- Empty state (task not yet executed): centered message "Task has not been executed yet" in Inter 14px, color `#5E5E65`.

**4. Create `src/components/modal/reply-input.tsx`**:

Props: `taskId: string`, `isVisible: boolean` (true when task status is "review" and questionText exists)

- A text input + send button at the bottom of the chat area
- Input: full width, Inter 14px, padding `12px 16px`, `backgroundColor: "#FFFFFF"`, ghost border `outline: 1px solid rgba(218, 193, 185, 0.3)`, borderRadius `0.5rem`. Focus: `outline-color: #93452A`.
- Send button: terracotta gradient, `Send` icon from lucide-react (or `ArrowUp`), borderRadius `0.375rem`, 36px square, positioned at right edge inside the input wrapper.
- On submit: POST to `/api/tasks/${taskId}/reply` with `{ message }`. Clear input. Optimistically add a `user_reply` LogEntry to local state.
- Enter submits, Shift+Enter for newline.
- Placeholder: "Reply to Claude..."
- When `isVisible` is false, the input is hidden (display: none).

**5. Create `src/components/modal/modal-sidebar.tsx`**:

Props: `task: Task`

- Right sidebar panel, width 320px, `backgroundColor: "#F6F3F1"` (surface-container-low)
- Contains:
  1. Task name (Epilogue 18px, fontWeight 600, color `#1B1C1B`) and description if present (Inter 14px, color `#5E5E65`, marginTop 8px)
  2. `<PanelStats task={task} />` (reuse existing)
  3. `<PanelOutputs taskId={task.id} />` (reuse existing)
  4. Error section (same as current task-detail-panel.tsx error display, with AlertCircle icon)
- Scrollable independently from chat area

**6. Create `src/components/modal/modal-header.tsx`**:

Props: `task: Task`, `onClose: () => void`

- Height 56px, padding `0 24px`, flex row, space-between
- Left: task title (Epilogue 16px, fontWeight 600) + status badge (same dot + label as PanelStats) + level badge (reuse LevelBadge)
- Right: Close button (X icon, same style as panel-header.tsx close button)
- Bottom edge: background color shift to `#EAE8E6` (1px height div, same as panel-header)

**7. Create `src/components/modal/task-modal.tsx`** -- the container:

- Full-screen overlay: `position: fixed`, `inset: 0`, z-index 100
- Background: `rgba(252, 249, 247, 0.8)` with `backdropFilter: "blur(12px)"` (glassmorphism from design system)
- Inner container: `position: fixed`, `inset: "24px"` (24px margin from edges), `backgroundColor: "#FFFFFF"`, `borderRadius: "0.75rem"`, `boxShadow: "0px 12px 32px rgba(147, 69, 42, 0.06)"`, flex column, overflow hidden
- Layout (flex column):
  1. `<ModalHeader>` at top
  2. Main area (flex row, flex: 1, overflow hidden):
     - Left: `<ModalChat>` (flex: 1)
     - Right: `<ModalSidebar>` (width 320px)
  3. `<ReplyInput>` at bottom of chat area (not bottom of modal -- inside the chat column)

- State management:
  - Fetch log entries on mount via GET `/api/tasks/${task.id}/logs`
  - Listen for SSE `task:log` events and append to local log entries state
  - Listen for SSE `task:question` events to show reply input
  - Track `questionText` state (set from SSE task:question, cleared on reply)

- Renders only when `selectedTaskId` is set (same trigger as current panel)
- Escape key closes modal (same as current panel)
- Entry animation: scale from 0.98 to 1 + opacity 0 to 1, 200ms ease-out (CSS transition)
  </action>
  <verify>
    <automated>cd "projects/briefs/command-centre" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/modal/task-modal.tsx contains `export function TaskModal`
    - src/components/modal/task-modal.tsx contains `position: "fixed"` and `inset: "24px"`
    - src/components/modal/modal-chat.tsx contains `isAutoScrolling` or `autoScroll`
    - src/components/modal/modal-chat.tsx contains `Jump to latest`
    - src/components/modal/chat-entry.tsx contains `tool_use` and `question` and `user_reply`
    - src/components/modal/reply-input.tsx contains `/api/tasks/` and `reply`
    - src/components/modal/modal-sidebar.tsx contains `PanelStats` and `PanelOutputs`
    - src/app/api/tasks/[id]/logs/route.ts contains `export async function GET`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Full-screen modal renders with chat view, auto-scroll, tool call cards, reply input, and metadata sidebar. Logs API serves entry history.</done>
</task>

<task type="auto">
  <name>Task 2: Wire modal into app, update SSE hook, replace panel with modal trigger</name>
  <files>
    projects/briefs/command-centre/src/store/task-store.ts
    projects/briefs/command-centre/src/hooks/use-sse.ts
    projects/briefs/command-centre/src/components/panel/task-detail-panel.tsx
    projects/briefs/command-centre/src/lib/event-bus.ts
  </files>
  <read_first>
    projects/briefs/command-centre/src/store/task-store.ts
    projects/briefs/command-centre/src/hooks/use-sse.ts
    projects/briefs/command-centre/src/components/panel/task-detail-panel.tsx
    projects/briefs/command-centre/src/components/modal/task-modal.tsx
    projects/briefs/command-centre/src/app/page.tsx
    projects/briefs/command-centre/src/lib/event-bus.ts
  </read_first>
  <action>
**1. Update use-sse.ts** to handle new event types:

Add `"task:question"` and `"task:log"` to the `taskEvents` array so SSE subscribes to them:
```typescript
const taskEvents = [
  "task:created", "task:updated", "task:deleted",
  "task:status", "task:progress", "task:output",
  "task:question", "task:log",
];
```

For `task:question` events, call `applySSEEvent(event)` as normal (the store will handle it).
For `task:log` events, also call `applySSEEvent(event)` -- add handling in the store.

**2. Update task-store.ts** to support log entries and question state:

Add to the store interface:
```typescript
logEntries: Record<string, LogEntry[]>;  // taskId -> entries
questionText: string | null;             // current question from Claude

fetchLogEntries: (taskId: string) => Promise<void>;
appendLogEntry: (taskId: string, entry: LogEntry) => void;
setQuestionText: (text: string | null) => void;
```

Add initial state: `logEntries: {}`, `questionText: null`

Add `fetchLogEntries` implementation:
```typescript
fetchLogEntries: async (taskId: string) => {
  try {
    const res = await fetch(`/api/tasks/${taskId}/logs`);
    if (!res.ok) return;
    const entries = await res.json();
    set((state) => ({
      logEntries: { ...state.logEntries, [taskId]: entries },
    }));
  } catch { /* silently fail */ }
},
```

Add `appendLogEntry` and `setQuestionText` implementations.

Update `applySSEEvent` to handle `task:question` and `task:log`:
```typescript
case "task:question":
  // Update the task in state
  set((state) => ({
    tasks: state.tasks.map((t) => t.id === event.task.id ? event.task : t),
    questionText: event.questionText || null,
  }));
  break;
case "task:log":
  // Append log entry -- event.logEntry contains the LogEntry
  if ((event as any).logEntry) {
    get().appendLogEntry(event.task.id, (event as any).logEntry);
  }
  break;
```

Also update the `TaskEvent` import to include the extended type, and update the event-bus TaskEvent type if needed to include `logEntry?: LogEntry`.

**3. Replace task-detail-panel.tsx** to render the full-screen modal instead:

Replace the entire content of `task-detail-panel.tsx` to import and render `<TaskModal>`:
```typescript
"use client";
import { TaskModal } from "../modal/task-modal";

export function TaskDetailPanel() {
  return <TaskModal />;
}
```
This preserves the existing import site (wherever TaskDetailPanel is rendered) while switching to the full-screen modal. The modal internally reads `selectedTaskId` from the store (same as the old panel did).

**4. Update event-bus.ts** -- add `"task:log"` to `TaskEventType` and add `logEntry` to `TaskEvent`:
```typescript
export type TaskEventType =
  | "task:created" | "task:updated" | "task:deleted"
  | "task:status" | "task:progress" | "task:output"
  | "task:question" | "task:log";

export interface TaskEvent {
  type: TaskEventType;
  task: Task;
  timestamp: string;
  questionText?: string;
  logEntry?: import("@/types/task").LogEntry;
}
```
  </action>
  <verify>
    <automated>cd "projects/briefs/command-centre" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/hooks/use-sse.ts contains `"task:question"` and `"task:log"` in taskEvents array
    - src/store/task-store.ts contains `logEntries:` and `questionText:`
    - src/store/task-store.ts contains `fetchLogEntries`
    - src/store/task-store.ts applySSEEvent handles `"task:question"` case
    - src/components/panel/task-detail-panel.tsx imports and renders `TaskModal`
    - src/lib/event-bus.ts contains `"task:log"` in TaskEventType
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Modal is wired into the app via the existing TaskDetailPanel mount point, SSE streams log entries and question events to the store, modal receives live updates</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Phase 6 implementation end-to-end</name>
  <action>Human verifies the complete Phase 6 implementation including task creation form, full-screen modal, live execution logs, and reply flow.</action>
  <what-built>Full Phase 6 implementation: task creation with name/description, real execution with stdin pipe, full-screen modal with chat-style logs, tool call cards, auto-scroll, and reply input for Claude questions</what-built>
  <how-to-verify>
    1. Start dev server: `cd projects/briefs/command-centre && npm run dev`
    2. Open http://localhost:3000
    3. **Task creation form:**
       - Click the input -- type a task name
       - Verify description textarea slides in below
       - Tab to description, type details
       - Verify Cmd+Enter submits
       - Clear form, type just a name, press Enter -- verify it submits immediately without description
       - Verify "Send to Claude" button appears with terracotta gradient
    4. **Full-screen modal:**
       - Click on any existing task card
       - Verify full-screen modal opens (not the old 480px slide-out)
       - Verify Escape closes it
       - Verify sidebar shows task metadata (stats, outputs)
    5. **Live execution (if Claude CLI available):**
       - Create a task and drag to Queued (or use Execute button)
       - Click the running task to open modal
       - Verify log entries stream in as chat bubbles
       - Verify tool calls show as collapsed one-line cards
       - Scroll up -- verify "Jump to latest" button appears
       - Click "Jump to latest" -- verify it scrolls to bottom
    6. **Reply flow (if Claude asks a question):**
       - When Claude asks a question, verify task moves to Review
       - Verify question shows with terracotta left border
       - Type a reply and send -- verify it appears as a right-aligned bubble
       - Verify task resumes Running
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd projects/briefs/command-centre && npx tsc --noEmit` -- zero errors
2. `ls src/components/modal/` -- shows task-modal.tsx, modal-header.tsx, modal-chat.tsx, modal-sidebar.tsx, chat-entry.tsx, reply-input.tsx
3. `grep -rn "task:question" src/` -- shows in event-bus, use-sse, task-store
4. `grep -rn "task:log" src/` -- shows in event-bus, use-sse, task-store, process-manager
5. `grep -n "TaskModal" src/components/panel/task-detail-panel.tsx` -- shows modal replaces panel
6. `ls src/app/api/tasks/*/logs/route.ts` -- logs API exists
</verification>

<success_criteria>
- Full-screen modal opens when clicking a task card
- Chat view renders log entries with proper styling per type (text, tool_use, question, user_reply)
- Auto-scroll follows new entries; "Jump to latest" appears when scrolled up
- Reply input appears when task is in Review with a question
- Sidebar shows task metadata via reused PanelStats and PanelOutputs
- Human verification confirms the visual flow works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-execution-and-detail-ui/06-03-SUMMARY.md`
</output>
