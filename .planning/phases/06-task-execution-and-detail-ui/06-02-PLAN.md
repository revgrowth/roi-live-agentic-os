---
phase: 06-task-execution-and-detail-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/briefs/command-centre/src/components/board/task-create-input.tsx
  - projects/briefs/command-centre/src/store/task-store.ts
autonomous: true
requirements:
  - EXEC-06-01

must_haves:
  truths:
    - "Typing in the task input reveals a description textarea below when user clicks or tabs into it"
    - "Pressing Enter on the name field with no description submits immediately"
    - "The form feels like instructing an AI agent, not naming a Kanban card"
    - "Description is optional -- quick task flow preserved"
  artifacts:
    - path: "projects/briefs/command-centre/src/components/board/task-create-input.tsx"
      provides: "Inline-expand task creation form with name + description fields"
      contains: "description"
    - path: "projects/briefs/command-centre/src/store/task-store.ts"
      provides: "Updated createTask that sends description to API"
      contains: "description"
  key_links:
    - from: "task-create-input.tsx"
      to: "task-store.ts"
      via: "createTask(title, description, level)"
      pattern: "createTask"
    - from: "task-store.ts"
      to: "/api/tasks POST"
      via: "fetch with description in body"
      pattern: "description"
---

<objective>
Replace the single-line task input with a Notion-inspired inline-expand form that captures both a task name and optional description.

Purpose: Per D-01 through D-04, the creation UX should feel like composing a prompt/command to Claude, not just naming a Kanban card. A short name becomes the card title; the description becomes the full brief sent to Claude.
Output: Redesigned task creation component, updated store with description support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-task-execution-and-detail-ui/06-CONTEXT.md

<interfaces>
<!-- Current task-create-input.tsx uses useTaskStore createTask(title, level) -->
<!-- Store calls POST /api/tasks with { title, level, clientId } -->
<!-- After Plan 01: API accepts { title, description, level, clientId } -->
<!-- After Plan 01: Task type has description: string | null -->

From src/store/task-store.ts:
```typescript
createTask: (title: string, level: TaskLevel) => Promise<void>;
// POST body: JSON.stringify({ title, level, clientId })
```

From src/types/task.ts (after Plan 01 updates):
```typescript
export interface TaskCreateInput {
  title: string;
  description?: string | null;
  level: TaskLevel;
  clientId?: string | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign task creation form with inline expand and update store</name>
  <files>
    projects/briefs/command-centre/src/components/board/task-create-input.tsx
    projects/briefs/command-centre/src/store/task-store.ts
  </files>
  <read_first>
    projects/briefs/command-centre/src/components/board/task-create-input.tsx
    projects/briefs/command-centre/src/store/task-store.ts
    projects/briefs/command-centre/src/types/task.ts
    .planning/phases/06-task-execution-and-detail-ui/06-CONTEXT.md
  </read_first>
  <action>
**1. Rewrite task-create-input.tsx** with the inline-expand pattern (per D-01, D-02, D-04):

Replace the current single-line input with a form that:
- Shows a spacious input field with placeholder "What would you like Claude to do?" (per D-04 -- feels like instructing an agent)
- When the name input is focused and non-empty, a description textarea slides in below with smooth height transition (max-height CSS transition, 200ms ease)
- Description textarea placeholder: "Add details, context, or specific instructions..." (per D-04)
- Description textarea is auto-growing (adjusts height to content, min 60px, max 160px)
- Enter on name field submits immediately if description area is not focused (per D-02)
- Tab from name field moves focus to description textarea
- Cmd/Ctrl+Enter from anywhere in the form submits
- A small "Send to Claude" submit button appears at bottom-right of the expanded form, styled as a terracotta gradient primary button (per design system: `background: linear-gradient(135deg, #93452A, #B25D3F)`, white text, `border-radius: 0.375rem`, padding `6px 16px`, font-size 13px, font-family Space Grotesk)
- Level selector chips remain (same style as current) but move inside the expanded area, positioned bottom-left
- The entire expanded form has a card-like appearance: `backgroundColor: "#FFFFFF"`, `borderRadius: "0.5rem"`, `boxShadow: "0px 12px 32px rgba(147, 69, 42, 0.06)"` (ambient shadow from design system), padding 16px
- When collapsed (empty name, not focused), show just the input line with ghost border: `outline: 1px solid rgba(218, 193, 185, 0.2)`, no shadow
- Focus state on name input: `outline-color: #93452A` at full opacity with `0 0 0 3px rgba(147, 69, 42, 0.1)` glow

State management:
- `title` (string), `description` (string), `level` (TaskLevel), `isExpanded` (boolean), `isSubmitting` (boolean)
- `isExpanded` becomes true when name input is focused and has text, or description has text
- On submit: call `createTask(title.trim(), description.trim() || null, level)`, then reset all fields and collapse

Typography per design system:
- Name input: Inter, 15px, color #1B1C1B, fontWeight 500
- Description textarea: Inter, 14px, color #1B1C1B, fontWeight 400
- Placeholder text: color #5E5E65
- Submit button label: Space Grotesk, 13px, fontWeight 600
- Level chips: Space Grotesk, 12px (same as current)

**2. Update task-store.ts** -- modify `createTask` signature and body:

Change the `createTask` interface type:
```typescript
createTask: (title: string, description: string | null, level: TaskLevel) => Promise<void>;
```

In the implementation:
- Accept `description` parameter
- Add `description` to the optimistic temp task: `description: description || null`
- Add `description` to the POST body: `JSON.stringify({ title, level, clientId, description })`

Also add `description` to the temp task creation object (the `tempTask` variable).
  </action>
  <verify>
    <automated>cd "projects/briefs/command-centre" && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/board/task-create-input.tsx contains `description`
    - src/components/board/task-create-input.tsx contains `What would you like Claude to do?`
    - src/components/board/task-create-input.tsx contains `Send to Claude`
    - src/components/board/task-create-input.tsx contains `isExpanded`
    - src/components/board/task-create-input.tsx contains `linear-gradient(135deg, #93452A, #B25D3F)`
    - src/store/task-store.ts createTask function accepts description parameter
    - src/store/task-store.ts POST body includes `description`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Task creation form shows inline-expand with name/description fields, feels like instructing Claude, preserves quick-submit on Enter, store sends description to API</done>
</task>

</tasks>

<verification>
1. `cd projects/briefs/command-centre && npx tsc --noEmit` -- zero errors
2. `grep -n "description" src/components/board/task-create-input.tsx` -- shows description state and textarea
3. `grep -n "isExpanded" src/components/board/task-create-input.tsx` -- shows expand/collapse logic
4. `grep -n "description" src/store/task-store.ts` -- shows description in createTask and POST body
</verification>

<success_criteria>
- TypeScript compiles with zero errors
- Task creation form has inline-expand pattern with name + description
- Description is optional -- Enter on name submits without description
- Store sends description in API call
- Form styling matches design system (terracotta gradient button, warm palette, no hard borders)
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-execution-and-detail-ui/06-02-SUMMARY.md`
</output>
